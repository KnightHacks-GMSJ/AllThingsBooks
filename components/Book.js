import { StatusBar } from "expo-status-bar";
import React from "react";
import { View, Text } from "react-native";

// This will be a book object that will contain:
// => Title, Author, publisher, date, description, page count, rating, and linktobuy

function Book() {
    return (
        <View>
            <Text>Book</Text>
        </View>
    );
}

export default Book;

import { StatusBar } from "expo-status-bar";
import React from "react";
import { View, Text } from "react-native";

function Book() {
    return (
        <View>
            <Text>Book</Text>
        </View>
    );
}

export default Book;
